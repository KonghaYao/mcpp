import { Hono } from "hono";import type { Db } from "../db/database.ts";import { assert } from "../errors.ts";import { configProjection } from "../services/catalog.ts";
const visible="i.status='active' AND l.revision_id IS NOT NULL";
export function publicRoutes(db:Db){const r=new Hono();r.get("/backends",c=>c.json({items:db.query("SELECT slug,display_name displayName,description,website_url websiteUrl FROM backend_registries WHERE status='active' ORDER BY slug").all()}));r.get("/backends/:slug",c=>{const x=db.query("SELECT slug,display_name displayName,description,website_url websiteUrl FROM backend_registries WHERE slug=? AND status='active'").get(c.req.param("slug"));assert(x,404,"BACKEND_NOT_FOUND");return c.json(x)});r.get("/items",c=>{const q=c.req.query(),limit=Math.min(Math.max(Number(q.limit)||50,1),100),like=`%${q.q??""}%`;return c.json({items:db.query(`SELECT i.slug,i.display_name displayName,i.summary,p.slug publisher,b.slug backend,r.version latestVersion FROM mcp_items i JOIN publishers p ON p.id=i.publisher_id JOIN backend_registries b ON b.id=i.backend_registry_id JOIN mcp_item_latest l ON l.item_id=i.id JOIN mcp_item_revisions r ON r.id=l.revision_id WHERE ${visible} AND (?='' OR b.slug=?) AND (?='' OR p.slug=?) AND (?='' OR i.slug LIKE ? OR i.display_name LIKE ? OR COALESCE(i.summary,'') LIKE ?) ORDER BY i.slug LIMIT ?`).all(q.backend??"",q.backend??"",q.publisher??"",q.publisher??"",q.q??"",like,like,like,limit)} )});const item=(slug:string)=>db.query(`SELECT i.slug,i.display_name displayName,i.summary,i.description,i.homepage_url homepageUrl,i.repository_url repositoryUrl,i.icon_url iconUrl,i.tags_json tagsJson,p.slug publisherSlug,p.display_name publisherName,b.slug backendSlug,b.display_name backendName,r.version latestVersion FROM mcp_items i JOIN publishers p ON p.id=i.publisher_id JOIN backend_registries b ON b.id=i.backend_registry_id JOIN mcp_item_latest l ON l.item_id=i.id JOIN mcp_item_revisions r ON r.id=l.revision_id WHERE i.slug=? AND ${visible}`).get(slug) as any;r.get("/items/:slug",c=>{const x=item(c.req.param("slug"));assert(x,404,"ITEM_NOT_FOUND");return c.json({slug:x.slug,displayName:x.displayName,summary:x.summary,description:x.description,backend:{slug:x.backendSlug,displayName:x.backendName},publisher:{slug:x.publisherSlug,displayName:x.publisherName},latestVersion:x.latestVersion,homepageUrl:x.homepageUrl,repositoryUrl:x.repositoryUrl,iconUrl:x.iconUrl,tags:JSON.parse(x.tagsJson)})});r.get("/items/:slug/revisions",c=>{assert(item(c.req.param("slug")),404,"ITEM_NOT_FOUND");return c.json({items:db.query("SELECT r.version,r.published_at publishedAt FROM mcp_item_revisions r JOIN mcp_items i ON i.id=r.item_id WHERE i.slug=? AND i.status='active' AND r.status='published' ORDER BY r.published_at DESC").all(c.req.param("slug"))})});const revision=(slug:string,version?:string)=>db.query(`SELECT i.slug,b.slug backend_slug,r.*,CASE WHEN l.revision_id=r.id THEN 1 ELSE 0 END is_latest FROM mcp_items i JOIN backend_registries b ON b.id=i.backend_registry_id JOIN mcp_item_latest l ON l.item_id=i.id JOIN mcp_item_revisions r ON r.item_id=i.id WHERE i.slug=? AND i.status='active' AND r.status='published' AND r.id=${version?"r.id":"l.revision_id"} ${version?"AND r.version=?":""}`).get(...(version?[slug,version]:[slug])) as any;r.get("/items/:slug/revisions/:version",c=>{const x=revision(c.req.param("slug"),c.req.param("version"));assert(x,404,"REVISION_NOT_FOUND");return c.json({item:x.slug,version:x.version,isLatest:!!x.is_latest,backend:x.backend_slug,backendLocator:JSON.parse(x.backend_locator_json),serverDefinition:JSON.parse(x.server_definition_json),envSchema:x.env_schema_json?JSON.parse(x.env_schema_json):null,publishedAt:x.published_at})});r.get("/items/:slug/config",c=>{const x=revision(c.req.param("slug"));assert(x,404,"ITEM_NOT_FOUND");return c.json(configProjection(x))});r.get("/items/:slug/revisions/:version/config",c=>{const x=revision(c.req.param("slug"),c.req.param("version"));assert(x,404,"REVISION_NOT_FOUND");return c.json(configProjection(x))});return r}
